export default {
  /**
   * Enforce linebreaks after opening and before closing array brackets.
   *
   * See: https://eslint.org/docs/latest/rules/array-bracket-newline
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L24
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'array-bracket-newline': 'off',

  /**
   * Enforce consistent spacing inside array brackets.
   *
   * See: https://eslint.org/docs/latest/rules/array-bracket-spacing
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L25
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'array-bracket-spacing': 'off',

  /**
   * Enforce line breaks after each array element.
   *
   * See: https://eslint.org/docs/latest/rules/array-element-newline
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L26
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'array-element-newline': 'off',

  /**
   * Require parentheses around arrow function arguments.
   *
   * See: https://eslint.org/docs/latest/rules/arrow-parens
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L27
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'arrow-parens': 'off',

  /**
   * Enforce consistent spacing before and after the arrow in arrow functions.
   *
   * See: https://eslint.org/docs/latest/rules/arrow-spacing
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L28
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'arrow-spacing': 'off',

  /**
   * Disallow or enforce spaces inside of blocks after opening block and before closing block.
   *
   * See: https://eslint.org/docs/latest/rules/block-spacing
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L29
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'block-spacing': 'off',

  /**
   * Enforce consistent brace style for blocks.
   *
   * See: https://eslint.org/docs/latest/rules/brace-style
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L30
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'brace-style': 'off',

  /**
   * Require or disallow trailing commas.
   *
   * See: https://eslint.org/docs/latest/rules/comma-dangle
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L31
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'comma-dangle': 'off',

  /**
   * Enforce consistent spacing before and after commas.
   *
   * See: https://eslint.org/docs/latest/rules/comma-spacing
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L32
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'comma-spacing': 'off',

  /**
   * Enforce consistent comma style.
   *
   * See: https://eslint.org/docs/latest/rules/comma-style
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L33
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'comma-style': 'off',

  /**
   * Enforce consistent spacing inside computed property brackets.
   *
   * See: https://eslint.org/docs/latest/rules/computed-property-spacing
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L34
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'computed-property-spacing': 'off',

  /**
   * Enforce consistent newlines before and after dots.
   *
   * See: https://eslint.org/docs/latest/rules/dot-location
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L35
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'dot-location': 'off',

  /**
   * Require or disallow newline at the end of files.
   *
   * See: https://eslint.org/docs/latest/rules/eol-last
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L36
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'eol-last': 'off',

  /**
   * Require or disallow spacing between function identifiers and their invocations.
   *
   * See: https://eslint.org/docs/latest/rules/func-call-spacing
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L37
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'func-call-spacing': 'off',

  /**
   * Enforce line breaks between arguments of a function call.
   *
   * See: https://eslint.org/docs/latest/rules/function-call-argument-newline
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L38
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'function-call-argument-newline': 'off',

  /**
   * Enforce consistent line breaks inside function parentheses.
   *
   * See: https://eslint.org/docs/latest/rules/function-paren-newline
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L39
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'function-paren-newline': 'off',

  /**
   * Enforce consistent spacing around `*` operators in generator functions.
   *
   * See: https://eslint.org/docs/latest/rules/generator-star-spacing
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L41
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'generator-star-spacing': 'off',

  /**
   * Enforce the location of arrow function bodies.
   *
   * See: https://eslint.org/docs/latest/rules/implicit-arrow-linebreak
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L42
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'implicit-arrow-linebreak': 'off',

  /**
   * Enforce consistent indentation.
   *
   * See: https://eslint.org/docs/latest/rules/indent
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L43
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  indent: 'off',

  /**
   * Enforce the consistent use of either double or single quotes in JSX attributes.
   *
   * See: https://eslint.org/docs/latest/rules/jsx-quotes
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L44
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'jsx-quotes': 'off',

  /**
   * Enforce consistent spacing between keys and values in object literal properties.
   *
   * See: https://eslint.org/docs/latest/rules/key-spacing
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L45
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'key-spacing': 'off',

  /**
   * Enforce consistent spacing before and after keywords.
   *
   * See: https://eslint.org/docs/latest/rules/keyword-spacing
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L46
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'keyword-spacing': 'off',

  /**
   * Enforce position of line comments.
   *
   * See: https://eslint.org/docs/latest/rules/line-comment-position
   *
   * Rationale: Choosing the best position for a comment depends upon the code being commented so
   *            applying a rule across the board results in potentially less-readable code.
   */
  'line-comment-position': 'off',

  /**
   * Enforce consistent linebreak style.
   *
   * See: https://eslint.org/docs/latest/rules/linebreak-style
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L47
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'linebreak-style': 'off',

  /**
   * Require empty lines around comments.
   *
   * See: https://eslint.org/docs/latest/rules/lines-around-comment
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/README.md#lines-around-comment
   *
   * Rationale: While technically compatible with Prettier, enforcing this rule can create hard to
   *            resolve issues where Prettier and ESLint disagree.
   */
  'lines-around-comment': 'off',

  /**
   * Require or disallow an empty line between class members.
   *
   * See: https://eslint.org/docs/latest/rules/lines-between-class-members
   *
   * Rationale: Keeping lines between class members makes for more readable code.
   */
  'lines-between-class-members': 'error',

  /**
   * Enforce a maximum line length.
   *
   * See: https://eslint.org/docs/latest/rules/max-len
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/README.md#max-len
   *
   * Rationale: While technically compatible with Prettier, enforcing this rule can create hard to
   *            resolve issues where Prettier and ESLint disagree.
   */
  'max-len': 'off',

  /**
   * Enforce a maximum number of statements allowed per line.
   *
   * See: https://eslint.org/docs/latest/rules/max-statements-per-line
   *
   * Rationale: Having more than one statement per line can make code harder to read.
   */
  'max-statements-per-line': 'error',

  /**
   * Enforce newlines between operands of ternary expressions.
   *
   * See: https://eslint.org/docs/latest/rules/multiline-ternary
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L48
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'multiline-ternary': 'off',

  /**
   * Enforce or disallow parentheses when invoking a constructor with no arguments.
   *
   * See: https://eslint.org/docs/latest/rules/new-parens
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L50
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'new-parens': 'off',

  /**
   * Require a newline after each call in a method chain.
   *
   * See: https://eslint.org/docs/latest/rules/newline-per-chained-call
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L49
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'newline-per-chained-call': 'off',

  /**
   * Disallow unnecessary parentheses.
   *
   * See: https://eslint.org/docs/latest/rules/no-extra-parens
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L53
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'no-extra-parens': 'off',

  /**
   * Disallow mixed spaces and tabs for indentation
   *
   * See: https://eslint.org/docs/latest/rules/no-mixed-spaces-and-tabs
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L56
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'no-mixed-spaces-and-tabs': 'off',

  /**
   * Disallow multiple spaces.
   *
   * See: https://eslint.org/docs/latest/rules/no-multi-spaces
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L57
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'no-multi-spaces': 'off',

  /**
   * Disallow multiple empty lines.
   *
   * See: https://eslint.org/docs/latest/rules/no-multiple-empty-lines
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L58
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'no-multiple-empty-lines': 'off',

  /**
   * Disallow all tabs.
   *
   * See: https://eslint.org/docs/latest/rules/no-tabs
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/README.md#no-tabs
   *
   * Rationale: Using tabs can indicate an attempt to align something visually within code. This
   *            risks not translating well between different developer environments due to default
   *            and user-defined settings for tab-width being different.
   */
  'no-tabs': 'error',

  /**
   * Disallow trailing whitespace at the end of lines.
   *
   * See: https://eslint.org/docs/latest/rules/no-trailing-spaces
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L61
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'no-trailing-spaces': 'off',

  /**
   * Disallow whitespace before properties.
   *
   * See: https://eslint.org/docs/latest/rules/no-whitespace-before-property
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L62
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'no-whitespace-before-property': 'off',

  /**
   * Enforce the location of single-line statements.
   *
   * See: https://eslint.org/docs/latest/rules/nonblock-statement-body-position
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L64
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'nonblock-statement-body-position': 'off',

  /**
   * Enforce consistent line breaks after opening and before closing braces.
   *
   * See: https://eslint.org/docs/latest/rules/object-curly-newline
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L65
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'object-curly-newline': 'off',

  /**
   * Enforce consistent spacing inside braces.
   *
   * See: https://eslint.org/docs/latest/rules/object-curly-spacing
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L66
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'object-curly-spacing': 'off',

  /**
   * Enforce placing object properties on separate lines.
   *
   * See: https://eslint.org/docs/latest/rules/object-property-newline
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L67
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'object-property-newline': 'off',

  /**
   * Enforce consistent linebreak style for operators.
   *
   * See: https://eslint.org/docs/latest/rules/operator-linebreak
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L69
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'operator-linebreak': 'off',

  /**
   * Require or disallow padding within blocks.
   *
   * See: https://eslint.org/docs/latest/rules/padded-blocks
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L70
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'padded-blocks': 'off',

  /**
   * Require or disallow padding lines between statements.
   *
   * See: https://eslint.org/docs/latest/rules/padding-line-between-statements
   *
   * Rationale: Having a blank line before a `return` statement makes code flow easier to
   *            understand.
   */
  'padding-line-between-statements': ['error', { blankLine: 'always', prev: '*', next: 'return' }],

  /**
   * Enforce the consistent use of either backticks, double, or single quotes.
   *
   * See: https://eslint.org/docs/latest/rules/quotes
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/README.md#quotes
   *
   * Rationale: While technically compatible with Prettier, enforcing this rule can create hard to
   *            resolve issues where Prettier and ESLint disagree.
   */
  quotes: 'off',

  /**
   * Enforce spacing between rest and spread operators and their expressions.
   *
   * See: https://eslint.org/docs/latest/rules/rest-spread-spacing
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L72
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'rest-spread-spacing': 'off',

  /**
   * Require or disallow semicolons instead of ASI.
   *
   * See: https://eslint.org/docs/latest/rules/semi
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L73
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  semi: 'off',

  /**
   * Enforce consistent spacing before and after semicolons.
   *
   * See: https://eslint.org/docs/latest/rules/semi-spacing
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L74
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'semi-spacing': 'off',

  /**
   * Enforce location of semicolons.
   *
   * See: https://eslint.org/docs/latest/rules/semi-style
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L75
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'semi-style': 'off',

  /**
   * Enforce consistent spacing before blocks.
   *
   * See: https://eslint.org/docs/latest/rules/space-before-blocks
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L78
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'space-before-blocks': 'off',

  /**
   * Enforce consistent spacing before `function` definition opening parenthesis.
   *
   * See: https://eslint.org/docs/latest/rules/space-before-function-paren
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L79
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'space-before-function-paren': 'off',

  /**
   * Enforce consistent spacing inside parentheses.
   *
   * See: https://eslint.org/docs/latest/rules/space-in-parens
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L83
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'space-in-parens': 'off',

  /**
   * Require spacing around infix operators.
   *
   * See: https://eslint.org/docs/latest/rules/space-infix-ops
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L84
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'space-infix-ops': 'off',

  /**
   * Enforce consistent spacing before or after unary operators.
   *
   * See: https://eslint.org/docs/latest/rules/space-unary-ops
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L86
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'space-unary-ops': 'off',

  /**
   * Enforce spacing around colons of switch statements.
   *
   * See: https://eslint.org/docs/latest/rules/switch-colon-spacing
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L88
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'switch-colon-spacing': 'off',

  /**
   * Require or disallow spacing around embedded expressions of template strings.
   *
   * See: https://eslint.org/docs/latest/rules/template-curly-spacing
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L89
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'template-curly-spacing': 'off',

  /**
   * Require or disallow spacing between template tags and their literals.
   *
   * See: https://eslint.org/docs/latest/rules/template-tag-spacing
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L90
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'template-tag-spacing': 'off',

  /**
   * Require or disallow Unicode byte order mark (BOM).
   *
   * See: https://eslint.org/docs/latest/rules/unicode-bom
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L91
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'unicode-bom': 'off',

  /**
   * Require parentheses around immediate `function` invocations.
   *
   * See: https://eslint.org/docs/latest/rules/wrap-iife
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L92
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'wrap-iife': 'off',

  /**
   * Require parenthesis around regex literals.
   *
   * See: https://eslint.org/docs/latest/rules/wrap-regex
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L93
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'wrap-regex': 'off',

  /**
   * Require or disallow spacing around the `*` in `yield*` expressions.
   *
   * See: https://eslint.org/docs/latest/rules/yield-star-spacing
   *
   * See (Prettier): https://github.com/prettier/eslint-config-prettier/blob/main/index.js#L94
   *
   * Rationale: Conflicts with Prettier (see above).
   */
  'yield-star-spacing': 'off',
};
